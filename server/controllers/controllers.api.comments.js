const Comment = require('../models/models.api.comments')

module.exports = {
    createComment: () => {

    },

    getAllcomments: (req, res) => {

    },

    deleteComment: (req, res) => {

    }
}